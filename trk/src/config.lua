-- config.lua (patched)
-- This file previously contained a path reference and was not valid Lua.
-- If you need configuration, define it here as a Lua table. Example:

local config = {
    -- Add configuration fields as needed
    -- example: data_path = "../data/"
}

return config